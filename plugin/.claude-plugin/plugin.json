{
  "name": "kilog",
  "description": "Inspect and query dev-time logs captured by kilog (.kilog/ per-project JSONL + DuckDB). Wraps the `kilog` CLI for log search, tail, browse, and maintenance.",
  "author": {
    "name": "kilog contributors"
  }
}
