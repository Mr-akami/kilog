---
"@kilog/kilog": patch
---

doc: rewrite README intro — lead with AI-agent log search; drop inaccurate "one place" wording. Per-project `.kilog/` storage, CLI scope = invocation dir (or `--root`), nothing centralized.
