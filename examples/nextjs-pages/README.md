# nextjs-pages

Minimal Next.js (Pages Router) demo wired to kilog via `withKilog` only.

```bash
pnpm install
pnpm dev          # next dev
pnpm kilog logs   # in another shell
```

The user's only kilog-related code is the `next.config.ts` wrap.
