{
  "name": "@logit/example-node-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "node --import @logit/runtime-node/register ./src/index.ts",
    "logit": "logit"
  },
  "dependencies": {
    "@hono/node-server": "*",
    "@logit/runtime-node": "workspace:*",
    "hono": "*"
  },
  "devDependencies": {
    "@logit/cli": "workspace:*"
  }
}
